import React from 'react';
import {
    Dimensions
} from 'react-native';

const Demens = {
    windowWidth: Dimensions.get('window').width,
    windowHeight: Dimensions.get('window').height
}

export default Demens;